import 'package:objectbox/objectbox.dart';

import '../model/UserDB.dart';

class ObjectBoxDB {
  _rty() {
    // final Store store = Store(getObjectBoxModel(), directory: "memory:test-db");
    // final box = store.box<UserDB>();

// Put a new object into the box
    // var person = UserDB(name: "Joe Green");
    // final id = box.put(person);
  }
}
